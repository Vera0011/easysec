[Unit]
Description=Keycloak Identity Provider - EasySec
After=network.target

[Service]
User={{ keycloak_user }}
Group={{ keycloak_group }}
ExecStart={{ keycloak_path }}/bin/kc.sh start
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target