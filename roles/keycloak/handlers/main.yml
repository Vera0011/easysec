---
- name: Reload daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Keycloak
  ansible.builtin.systemd:
    name: "{{ keycloak_package_name }}"
    state: restarted
    enabled: true
