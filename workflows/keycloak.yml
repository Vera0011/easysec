---
- name: Installs Keycloak on test host (Vagrant)
  hosts: vagrant-keycloak-1
  become: true
  roles:
    - keycloak
  vars:
    keycloak_database:
      ssl:
        provider: "selfsigned"

    keycloak_ssl:
      provider: "selfsigned"
